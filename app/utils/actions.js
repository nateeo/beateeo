// export actions and write reducers here for brevity

const UPDATE_VOLUME = 'UPDATE_VOLUME'

const reducer = (state, action) => {
  switch (action.type) {
    case UPDATE_VOLUME:
      break
  }
}

export default reducer
export { UPDATE_VOLUME }
